enable-ssh-support
# TODO: host-specific — pinentry-gnome3 only exists where a GNOME/GTK desktop is
# installed, so shipping it universally is wrong for headless/other hosts. Should
# live behind rcm's host/tag mechanism, not the universal config; work out the
# right split when the multi-host setup is ready (gpg-agent.conf has no include,
# so it can't use the .local sourcing trick the shell configs do).
pinentry-program /usr/bin/pinentry-gnome3
